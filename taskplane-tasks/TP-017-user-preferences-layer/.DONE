Task TP-017 completed 2026-03-17

Summary:
- Implemented user preferences layer at ~/.pi/agent/taskplane/preferences.json
- Added resolveUserPreferencesPath(), loadUserPreferences(), applyUserPreferences()
- Preferences auto-created with defaults on first access
- Layer 2 fields (operatorId, mergeModel, tmuxPrefix) override project config
- dashboardPort stored in preferences for future TUI consumption
- 461 tests passing across 17 test files
